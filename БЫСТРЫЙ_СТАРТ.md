# ⚡ БЫСТРЫЙ СТАРТ

## 🚀 ЧТО ДЕЛАТЬ ПРЯМО СЕЙЧАС

### 1️⃣ Запустите автоматический деплой

```powershell
cd C:\Users\Babur\Desktop\Avelon-Web-main
.\Добавить-Функционал-Бота.ps1
```

### 2️⃣ Подождите завершения (~2-3 минуты)

Скрипт автоматически:
- ✅ Скопирует файлы на VDS
- ✅ Зарегистрирует новые команды
- ✅ Пересоберёт приложение
- ✅ Перезапустит сервисы

### 3️⃣ Проверьте в Discord

```
/balance
/addbalance user:admin@eqwzzx.wtf amount:100 reason:Тест
/profile user:admin@eqwzzx.wtf
```

---

## 🎯 НОВЫЕ ВОЗМОЖНОСТИ

### Для администраторов:

| Команда | Описание | Пример |
|---------|----------|--------|
| `/addbalance` | Добавить баланс | `/addbalance user:admin@eqwzzx.wtf amount:500 reason:Бонус` |
| `/removebalance` | Убавить баланс | `/removebalance user:admin@eqwzzx.wtf amount:100 reason:Штраф` |
| `/profile` | Просмотр профиля | `/profile user:admin@eqwzzx.wtf` |

### Для всех пользователей:

| Команда | Описание |
|---------|----------|
| `/balance` | Проверить свой баланс |

---

## ✨ НОВАЯ ЛОГИКА СОЗДАНИЯ СЕРВЕРОВ

| Условие | Результат |
|---------|-----------|
| ✅ Discord привязан | Email верифицировать **не обязательно** |
| ❌ Discord не привязан + Email не верифицирован | ⛔ Сервер создать **нельзя** |
| ❌ Discord не привязан + "Обязательная привязка" включена | ⛔ Сервер создать **нельзя** |

---

## ⚙️ ЧТО ПРОВЕРИТЬ ПОСЛЕ ДЕПЛОЯ

### На VDS:

```bash
ssh root@77.91.100.68

# Проверить статус
pm2 status

# Посмотреть логи бота
pm2 logs fluxor-bot --lines 30

# Проверить конфигурацию админов
cat /var/www/billing/discord-bot/.env | grep ADMIN_DISCORD_IDS
```

**Должно быть:**
```
ADMIN_DISCORD_IDS=808641359006400512
```

---

## 🆘 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Команды не появились в Discord:

```bash
ssh root@77.91.100.68
cd /var/www/billing/discord-bot
node deploy-commands.js
```

Подождите 2-5 минут (Discord кэширует команды)

### Ошибка "Доступ запрещён":

Ваш Discord ID не указан в `ADMIN_DISCORD_IDS`

```bash
ssh root@77.91.100.68
nano /var/www/billing/discord-bot/.env
```

Добавьте свой Discord ID:
```env
ADMIN_DISCORD_IDS="ваш_discord_id"
```

Перезапустите:
```bash
pm2 restart fluxor-bot
```

### Логика создания серверов не работает:

Пересоберите веб-приложение:
```bash
ssh root@77.91.100.68
cd /var/www/billing
rm -rf .next
npm run build
pm2 restart fluxor-web
```

---

## 📊 МОНИТОРИНГ

```bash
# Статус сервисов
pm2 status

# Логи бота (реал-тайм)
pm2 logs fluxor-bot

# Логи веб-приложения (реал-тайм)
pm2 logs fluxor-web

# Последние ошибки
pm2 logs fluxor-bot --err --lines 50
```

---

## 📚 ПОДРОБНАЯ ДОКУМЕНТАЦИЯ

Смотрите файлы:
- `ГОТОВО_К_ДЕПЛОЮ.md` - Полное руководство по деплою
- `НОВЫЕ_КОМАНДЫ_БОТА.md` - Детальное описание команд
- `Добавить-Функционал-Бота.ps1` - Скрипт автоматического деплоя

---

**✨ ГОТОВО! ЗАПУСКАЙТЕ ДЕПЛОЙ! ✨**

```powershell
.\Добавить-Функционал-Бота.ps1
```
