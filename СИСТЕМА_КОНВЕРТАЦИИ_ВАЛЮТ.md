# 💱 Система автоматической конвертации валют

## 📋 Описание

Создана централизованная система конвертации валют, которая **автоматически конвертирует** цены из рублей в другие валюты на основе установленных курсов.

---

## ✨ Преимущества новой системы

### До:
- ❌ Нужно было вручную указывать цену для каждой валюты
- ❌ При изменении цены нужно было обновлять 4 значения
- ❌ Риск ошибок и несоответствий
- ❌ Сложно поддерживать актуальность

### После:
- ✅ **Одна цена в рублях** - автоматическая конвертация в остальные валюты
- ✅ Легко обновлять цены - меняем только рубли
- ✅ Легко обновлять курсы - меняем в одном месте
- ✅ Нет ошибок и несоответствий
- ✅ Простая поддержка

---

## 🏗️ Архитектура

### Файлы системы:

```
lib/
  └── currency.ts                    # Утилиты конвертации (главный файл)

app/api/
  └── currency/
      └── rates/
          └── route.ts               # API endpoint для получения курсов

components/
  └── pricing.tsx                     # Использует систему конвертации
```

---

## 📊 Текущие курсы валют

Курсы установлены в файле `lib/currency.ts`:

```typescript
export const CURRENCY_RATES = {
  RUB: 1,
  USD: 0.013,   // ~77 RUB за 1 USD
  EUR: 0.0115,  // ~87 RUB за 1 EUR
  UAH: 0.52,    // ~1.92 RUB за 1 UAH
}
```

### Как рассчитаны курсы:

На основе реальных данных из файла ТАРИФЫ:
- **199 ₽ = $2.59** → курс USD: `2.59 / 199 = 0.013`
- **199 ₽ = €2.29** → курс EUR: `2.29 / 199 = 0.0115`
- **199 ₽ = 104 ₴** → курс UAH: `104 / 199 = 0.52`

---

## 🔧 Использование

### В компонентах:

```typescript
import { formatPrice, type Currency } from '@/lib/currency'

// Цена в рублях
const priceRUB = 199

// Автоматическая конвертация и форматирование
const priceUSD = formatPrice(priceRUB, 'USD')  // "$2.59"
const priceEUR = formatPrice(priceRUB, 'EUR')  // "€2.29"
const priceUAH = formatPrice(priceRUB, 'UAH')  // "104 ₴"
const priceRUB = formatPrice(priceRUB, 'RUB')  // "199 ₽"
```

### Только конвертация (без форматирования):

```typescript
import { convertPrice } from '@/lib/currency'

const priceRUB = 199
const priceUSD = convertPrice(priceRUB, 'USD')  // 2.587
```

---

## 📝 Структура тарифов

### Старая структура (усложненная):

```typescript
{
  name: "Кролик",
  price: 199,
  priceUSD: 2.59,   // ❌ нужно указывать
  priceEUR: 2.29,   // ❌ нужно указывать
  priceUAH: 104     // ❌ нужно указывать
}
```

### Новая структура (упрощенная):

```typescript
{
  name: "Кролик",
  price: 199  // ✅ только рубли, остальное автоматически
}
```

---

## 🔄 Обновленные цены

### Minecraft тарифы:

| Тариф | RUB | USD (авто) | EUR (авто) | UAH (авто) |
|-------|-----|------------|------------|------------|
| Кролик | 199 ₽ | $2.59 | €2.29 | 104 ₴ |
| Овца | 299 ₽ | $3.89 | €3.44 | 155 ₴ |
| Волк | 399 ₽ | $5.19 | €4.59 | 207 ₴ |
| Панда | 599 ₽ | $7.79 | €6.89 | 311 ₴ |
| Белый медведь | 799 ₽ | $10.39 | €9.19 | 415 ₴ |
| Страж | 999 ₽ | $12.99 | €11.49 | 519 ₴ |
| Эндер-дракон | 1199 ₽ | $15.59 | €13.79 | 623 ₴ |
| Иссушитель | 1599 ₽ | $20.79 | €18.39 | 831 ₴ |
| Варден | 2499 ₽ | $32.45 | €28.72 | 1299 ₴ |

### Coding тарифы:

| Тариф | RUB | USD (авто) | EUR (авто) | UAH (авто) |
|-------|-----|------------|------------|------------|
| Старт | 99 ₽ | $1.29 | €1.14 | 51 ₴ |
| Разработка | 199 ₽ | $2.59 | €2.29 | 104 ₴ |
| Продакшн | 399 ₽ | $5.19 | €4.59 | 207 ₴ |
| Масштаб | 799 ₽ | $10.39 | €9.19 | 415 ₴ |
| Дата-центр | 1499 ₽ | $19.47 | €17.23 | 779 ₴ |

---

## 🛠️ Обновление курсов валют

### Способ 1: Вручную (текущий)

Отредактируйте файл `lib/currency.ts`:

```typescript
export const CURRENCY_RATES = {
  RUB: 1,
  USD: 0.013,   // Обновите здесь
  EUR: 0.0115,  // Обновите здесь
  UAH: 0.52,    // Обновите здесь
}
```

### Способ 2: Через API (будущее)

Можно подключить внешний API для автоматического обновления курсов:

```typescript
// Пример: получение курсов с ЦБ РФ
async function updateRates() {
  const response = await fetch('https://www.cbr-xml-daily.ru/daily_json.js')
  const data = await response.json()
  
  CURRENCY_RATES.USD = 1 / data.Valute.USD.Value
  CURRENCY_RATES.EUR = 1 / data.Valute.EUR.Value
  // И так далее...
}
```

---

## 📡 API Endpoint

### GET `/api/currency/rates`

Возвращает текущие курсы валют.

**Ответ:**

```json
{
  "rates": {
    "RUB": 1,
    "USD": 0.013,
    "EUR": 0.0115,
    "UAH": 0.52
  },
  "lastUpdated": "2026-07-14T12:00:00.000Z"
}
```

**Использование:**

```typescript
const response = await fetch('/api/currency/rates')
const { rates } = await response.json()

// rates.USD = 0.013
```

---

## 🎯 Примеры использования

### Пример 1: Добавление нового тарифа

```typescript
// Старый способ (сложно)
const newPlan = {
  name: "Новый тариф",
  price: 500,
  priceUSD: 6.50,    // нужно рассчитать
  priceEUR: 5.75,    // нужно рассчитать
  priceUAH: 260      // нужно рассчитать
}

// Новый способ (легко)
const newPlan = {
  name: "Новый тариф",
  price: 500  // всё, готово! ✅
}
```

### Пример 2: Изменение цены

```typescript
// Старый способ (сложно)
plan.price = 600
plan.priceUSD = 7.80      // нужно пересчитать
plan.priceEUR = 6.90      // нужно пересчитать
plan.priceUAH = 312       // нужно пересчитать

// Новый способ (легко)
plan.price = 600  // всё, готово! ✅
```

### Пример 3: Обновление курса валют

```typescript
// Старый способ (очень сложно)
// Нужно обновить priceUSD, priceEUR, priceUAH для КАЖДОГО тарифа

// Новый способ (легко)
// Обновляем только в lib/currency.ts:
CURRENCY_RATES.USD = 0.014  // готово! ✅
```

---

## 🧪 Тестирование

### Проверка конвертации:

```typescript
import { convertPrice, formatPrice } from '@/lib/currency'

// Тест 1: Конвертация
console.log(convertPrice(199, 'USD'))  // 2.587
console.log(convertPrice(199, 'EUR'))  // 2.2885
console.log(convertPrice(199, 'UAH'))  // 103.48

// Тест 2: Форматирование
console.log(formatPrice(199, 'RUB'))  // "199 ₽"
console.log(formatPrice(199, 'USD'))  // "$2.59"
console.log(formatPrice(199, 'EUR'))  // "€2.29"
console.log(formatPrice(199, 'UAH'))  // "104 ₴"
```

---

## 📋 Изменения в файлах

### Создано:

1. **`lib/currency.ts`** - Утилиты конвертации валют
2. **`app/api/currency/rates/route.ts`** - API endpoint

### Изменено:

1. **`components/pricing.tsx`** - Использует новую систему

---

## 🔒 Особенности форматирования

### Правила округления:

- **RUB** (₽) - без десятичных знаков (199 ₽)
- **USD** ($) - 2 десятичных знака ($2.59)
- **EUR** (€) - 2 десятичных знака (€2.29)
- **UAH** (₴) - без десятичных знаков (104 ₴)

### Порядок символов:

- **RUB**: `199 ₽` - число + пробел + символ
- **USD**: `$2.59` - символ + число
- **EUR**: `€2.29` - символ + число
- **UAH**: `104 ₴` - число + пробел + символ

---

## 🚀 Будущие улучшения

### Возможные доработки:

1. **Автоматическое обновление курсов**
   - Подключить API ЦБ РФ или другой источник
   - Обновлять курсы каждый день автоматически

2. **Кеширование курсов**
   - Хранить курсы в Redis или базе данных
   - Уменьшить количество запросов к внешним API

3. **История курсов**
   - Сохранять изменения курсов
   - Аналитика изменения цен

4. **Админ-панель**
   - Управление курсами через интерфейс
   - Просмотр истории изменений

---

## 💡 Советы

### Когда обновлять курсы:

- ✅ При значительном изменении курса (>5%)
- ✅ Раз в неделю/месяц для актуальности
- ✅ При добавлении новых валют

### Как проверить правильность курсов:

```bash
# Проверьте реальный курс
# 199 RUB → USD

# Ожидаемое значение по файлу ТАРИФЫ: $2.59
# Рассчитанное: 199 * 0.013 = 2.587 ✅

# Если значения не совпадают - обновите курс
```

---

## 📞 Использование в других местах

Система конвертации может использоваться не только для тарифов:

```typescript
import { formatPrice } from '@/lib/currency'

// Баланс пользователя
const balance = 1000
const balanceFormatted = formatPrice(balance, currency)

// Стоимость продления
const renewalCost = 500
const renewalFormatted = formatPrice(renewalCost, currency)

// Возврат средств
const refund = 250
const refundFormatted = formatPrice(refund, currency)
```

---

## ✅ Итог

### Что получили:

- ✅ **Централизованная система** конвертации валют
- ✅ **Автоматическая конвертация** из рублей
- ✅ **API endpoint** для получения курсов
- ✅ **Простое обновление** цен и курсов
- ✅ **Единый источник правды** для курсов
- ✅ **Легкая поддержка** и расширение

### Теперь для изменения цены нужно:

1. Изменить только **цену в рублях**
2. Всё остальное - **автоматически**!

---

**Дата создания:** 14 июля 2026  
**Версия:** 1.0.0  
**Статус:** ✅ Готово к использованию
